import { Typography, Grid } from "@material-ui/core"
import React from "react"

export default function PinkEye(){
    return(
        <Grid>
            <Typography variant="h3"> Pink eye page</Typography>
            <Typography variant="body1"> Pink eye crap</Typography>
        </Grid>
    )
}