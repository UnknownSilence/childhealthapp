import { Typography, Grid } from "@material-ui/core"
import React from "react"

export default function Diabetes(){
    return(
        <Grid>
            <Typography variant="h3"> Diabetes page</Typography>
            <Typography variant="body1"> Diabetes crap</Typography>
        </Grid>
    )
}