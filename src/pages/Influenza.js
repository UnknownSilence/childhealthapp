import { Typography, Grid } from "@material-ui/core"
import React from "react"

export default function Influenza(){
    return(
        <Grid>
            <Typography variant="h3"> Influenza page</Typography>
            <Typography variant="body1"> Influenza crap</Typography>
        </Grid>
    )
}